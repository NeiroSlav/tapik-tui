# from store import RootStore


# async def handle_login(username: str, password: str):
#     users = [u for u in soot_store.users.get_users() if u.username == username]
#     if not users:
#         return

#     soot_store.current_user_id.set(users[0].user_id)
